GREETING = "Hello from lib"
VERSION = "1.0.0"

def lib_helper(x):
    return x * 2

def lib_process(items):
    result = []
    for item in items:
        result.append(lib_helper(item))
    return result
